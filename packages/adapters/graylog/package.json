{
  "name": "@liquescent/log-correlator-graylog",
  "version": "0.0.7",
  "description": "Graylog adapter for log-correlator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "jest --passWithNoTests",
    "test:integration": "jest --testMatch='**/test/integration/**/*.test.ts' --testTimeout=60000",
    "test:integration:detectOpenHandles": "jest --testMatch='**/test/integration/**/*.test.ts' --testTimeout=60000 --detectOpenHandles"
  },
  "keywords": [
    "graylog",
    "logs",
    "adapter"
  ],
  "author": "",
  "license": "AGPL-3.0",
  "dependencies": {
    "@liquescent/log-correlator-core": "file:../../core",
    "node-fetch": "^2.7.0",
    "socks-proxy-agent": "^8.0.5"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.10.0",
    "@types/node-fetch": "^2.6.0",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.4",
    "typescript": "^5.2.2"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
